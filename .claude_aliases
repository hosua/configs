#!/bin/bash
# Managed by claude-tui's install.sh (/home/hoswoo/dev/claude-tui).
# The claude-* alias family (mcpick, profiles, tutor/explainer/yolo, etc.)
# now lives in ~/.config/claude-tui/commands.yaml as a numbered menu — run
# `claude` with no arguments to see it. Edit that file to add more entries.

alias claude="claude-tui"
alias cc="/home/hoswoo/.nvm/versions/node/v24.11.0/bin/claude --model opusplan"
