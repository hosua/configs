export LIVECOIN_API_KEYS=""  # comma-separated list of API keys
export CURRENCY="USD"
export MAIN_COIN="BTC"

# Database (DB_PATH only needed for Docker; code defaults to ../crypto.db)
export DEDUP_EPSILON="0"
